{% for user in users %}
    <a href="{{ path('profile', {'id': user.id}) }}">{{ user.username }}</a>
    <a href="{{ path('user_stats', {'id': user.id}) }}">Stats</a>
    <a href="{{ path('user_listened', {'id': user.id}) }}">Listened</a><br>
{% endfor %}